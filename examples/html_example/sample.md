# 示例文件说明

本目录包含 mdtool 库的使用示例文件。

## 文件列表

- `sample.md` - 示例 Markdown 源文件
- `custom.css` - 自定义 CSS 样式文件
- `html_example.py` - HTML 转换示例代码
- `examples/sample.html` - 转换后的 HTML 文件（自动生成）

## 使用说明

运行示例代码：

```bash
python html_example.py
```

这将把 `sample.md` 转换为 `sample.html`。
